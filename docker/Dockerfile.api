FROM parakeet-asr:nemo-2.7.3-jetson

RUN python3 -m pip install --no-cache-dir \
    "fastapi==0.116.1" \
    "uvicorn==0.35.0" \
    "python-multipart==0.0.20" \
    && python3 -m pip check

WORKDIR /workspace

CMD ["/bin/bash"]
